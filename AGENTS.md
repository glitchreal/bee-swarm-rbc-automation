# Repository Workflow

- Treat `rbc_quest_detector_no_atlas (1).lua` as the primary script.
- After editing any project file, verify the change, commit it, and automatically push it to `origin` (`https://github.com/glitchreal/bee-swarm-rbc-automation.git`) unless the user explicitly says not to push.
- Preserve unrelated user changes when preparing commits.
